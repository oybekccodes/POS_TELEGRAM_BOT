[phases.setup]
nixPkgs = ["zbar", "python313"]

[phases.install]
cmds = ["pip install -r requirements.txt"]
```

**Also add this to your `requirements.txt`** — replace `pyzbar` with a specific version:
```
python-telegram-bot==20.7
requests
psycopg2-binary
pyzbar==0.1.9
Pillow
